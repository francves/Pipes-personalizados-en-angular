import { ConvertirEuroADolarPipe } from './convertir-euro-adolar.pipe';

describe('ConvertirEuroADolarPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirEuroADolarPipe();
    expect(pipe).toBeTruthy();
  });
});
